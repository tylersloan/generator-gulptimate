{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.0",
  "dependencies": {<% if (includeBootstrap) { if (includeSass) { %>
    "bootstrap-sass": "twbs/bootstrap-sass#~3.1.1",<% } else { %>
    "bootstrap": "~3.1.1",<% }} if (includeModernizr) { %>
    "modernizr": "~2.6.2",<% } %>
    "jquery": "~1.11.0"
  },
  "devDependencies": {}
}